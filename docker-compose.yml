services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app
      - ./widget:/app/widget
      - ./google-credentials.json:/app/google-credentials.json:ro
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
